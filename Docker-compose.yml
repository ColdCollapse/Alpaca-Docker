services:
  Red-WS-Pub:
    build:
      context: .
      dockerfile: Red-WS-Publisher/Dockerfile
    container_name: Red-WS-Publisher
    ports:
      - "6379:6379"
    networks:
      - redis-net

networks:
  redis-net:
    driver: bridge
